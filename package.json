{
  "private": false,
  "name": "samushi-global-settings",
  "version": "1.0.3",
  "description": "Vue Plugin which make easy to seperate your global settings from vuex modules",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bili --name index --plugin vue --vue.css false"
  },
  "files": [
    "dist/*"
  ],
  "author": "Sami Maxhuni",
  "license": "MIT",
  "devDependencies": {
    "bili": "^5.0.5",
    "rollup-plugin-vue": "^6.0.0",
    "vue-template-compiler": "^2.6.12"
  }
}
